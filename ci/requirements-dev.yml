channels:
    - conda-forge
    - nodefaults
dependencies:
    - python = 3.11

    - numpy >= 1.26
    - scipy

    - boost-histogram >= 1.4.1

    - xarray >= 2024.5
    - dask >= 2024.5

    - sphinx == 8.2.*
    - sphinx-book-theme == 1.1.*
    - nbpshinx

    - ruff
    - mypy >= 1.10
    - pytest >= 7.4
